{
  "name": "quake0day-blog",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "translate": "node scripts/translate-posts.mjs",
    "translate:force": "node scripts/translate-posts.mjs --force",
    "publish:cf": "pnpm run translate && pnpm build && find dist -type f -size +24M -print -delete && npx wrangler pages deploy dist --project-name=quake0day-blog --branch=main --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^6.1.6",
    "@astrojs/rss": "^4.0.9"
  }
}
